# Tasks: Edge Security Shield

- [x] Create SDD proposal, spec, and design docs
- [ ] Create `src/hooks.server.js` with `sequence()` chain
- [ ] Implement `botAndScanBlocker` hook (User-Agent regex + scanner paths regex)
- [ ] Implement `rateLimiter` hook for `/api/checkout`, `/api/login`, `/api/registro-invitacion`
- [ ] Implement `securityHeaders` hook for response headers
- [ ] Validate project build with `npm run build`
